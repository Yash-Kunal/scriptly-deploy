{
  "name": "mern-deploy-app",
  "version": "1.0.0",
  "description": "A full-stack MERN application deployed on Render.",
  "scripts": {
    "start": "concurrently \"npm run start --prefix client\" \"npm run start --prefix server\"",
    "build": "npm run build --prefix client",
    "deploy": "npm run build && npm run start"
  },
  "dependencies": {
    "concurrently": "^6.0.0"
  },
  "devDependencies": {
    "vite": "^2.0.0"
  },
  "author": "Your Name",
  "license": "MIT"
}